# -*- coding: utf-8 -*-
"""BERT_Model_Embedding_Vector_Understabding.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1oWbP0dGkReyB9oGOTQ55NO0waL_uRQD7

**2017**

       Google introduced Transformer architecture
       paper : Attention all you need
       Machine translation : One language to another language
       Replace by : RNNS
       No one use RNNS

**2018:**

      Google developed BERT
      So many NLP model developed using this concept
      QA, Classification,Summary,NER
      BERT : RoBERTa, ALBERT, DistiliBERT (model)

**2019:**

     Huggingface Launches transformers library
     load a pretrained models :  2M models are there now

**2020:**

    GPT3  OpenAI introduces GPT3
    LLM concept evolved

**2021-2023**

          Google : T5
          Meta : llama
          Hugginggace : support all models

**2023:**

          ChatGPT era
          Huggingface : support
          so many models  ==== AI is established

| **Task**                           | **Common Transformers Classes**                                                                            | **Description**                                                  |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| **Sentiment analysis**             | `BertForSequenceClassification`, `DistilBertForSequenceClassification`, `RobertaForSequenceClassification` | Classifies text as positive/negative or multi-class sentiment.   |
| **Text generation (English)**      | `GPT2LMHeadModel`, `AutoModelForCausalLM`                                                                  | Generates text given a prompt (causal language modeling).        |
| **Named entity recognition (NER)** | `BertForTokenClassification`, `RobertaForTokenClassification`                                              | Labels each token with an entity type (person, location, etc.).  |
| **Question answering**             | `BertForQuestionAnswering`, `RobertaForQuestionAnswering`, `DistilBertForQuestionAnswering`                | Extracts answer spans from a given context.                      |
| **Filling masked text**            | `BertForMaskedLM`, `RobertaForMaskedLM`                                                                    | Predicts masked tokens in a sentence (masked language modeling). |
| **Summarization**                  | `BartForConditionalGeneration`, `T5ForConditionalGeneration`                                               | Generates a summary from long text.                              |
| **Translation**                    | `MarianMTModel`, `T5ForConditionalGeneration`, `MBartForConditionalGeneration`                             | Translates text between languages.                               |
| **Feature extraction**             | `BertModel`, `RobertaModel`, `DistilBertModel`                                                             | Returns hidden states/tensor embeddings of text.                 |
"""

!pip install transformers

"""## Use case

- Understanding the model

- Here we are not doing any task

- Here we read the model

- We understand about tokenizer,vocabulary and embeddings
"""

from transformers import BertModel, BertTokenizer
# Every model has its own tokenizer
# Its own words dictinary , it is called vocabulary
# we need to create the Embeddings using tokenizer and vocab
# in order to load the model : BertModel
# in order to load the tokenizer: BertTokenizer

"""- Every model will have

    - Tokenizer

    - Vocabulary

- Vocabulary is words list nothing but a Dictionary

- Tokenizer is used to token the sentence

- Which means divide the sentence into words also called as tokens

- those tokens or words should be available in **vocabulary**

- BertModel is used to access the Models

- BertTokenizer is used to get the model vocabulary

access the model

model name is : **bert-base-uncased**

- **bert base** :

 - 110M parameters

 - 12 layers

 - 768 Neurons

- **bert large** :

 - 340M Parameters

 - 24 layers

 - 1024 Neurons
"""

model=BertModel.from_pretrained('bert-base-uncased')
# model name is = 'bert-base-uncased'

# from transformers import pipeline
# unmasker = pipeline('fill-mask', model='bert-base-uncased')
# unmasker("Hello I'm a [MASK] model.")

dir(model)

model.framework
model.name_or_path
model.num_parameters()
model.base_model  # Embeddings Encoder pooler
model.embeddings
model.Encoder
model.pooler

model.framework

# pt = pytorch framework
# DL NLP
# tf pt

"""- By defualt Hugging face transformers based on pytorch

- if we want to use tensorflow then class will be change
"""

# from transformers import TFBertModel, BertTokenizer
# tfmodel=TFBertModel.from_pretrained('bert-base-uncased')
# tfmodel.framework  # Deprecated

model.generation_config  # No output

model.name_or_path

model.num_parameters()  # 110M

model.base_model
# 30522= vocabulary list  there are 30522 tokens
# 768= vector size , each token vector length is 768
# each token represnt as 768 size of vector

# position_embeddings: 512 : any sentence will truncate to 512 tokens only
# the maximum tokens are only 512

# token_type_embeddings : sentence pair
# at a time it can make it as only two sentences

"""- It will take each and every word

- It will give the word embeddings of each word

- Assume that a sentence has 100 words

- will get 100 word embeddings each vector has 768 values

- 100 words means   (100,768)

- Will apply one pool layer : similar analogy average of all vectors === activation function

- Again the final sentence word embeddings is a size of 768
"""

# directory embedding
model.embeddings

model.encoder

# for i in model.named_parameters():
#     print(i)

#for i in model.naparameters():
    #print(i)

#for i in model.named_parameters():
    #print(i)

# coding round  sql
# ML : Resume
# Manger round

"""## Bert Tokenizer"""

sentence='we are learning bert embeddings'

tokenizer=BertTokenizer.from_pretrained('bert-base-uncased')
tokenizer

"""- SEP token is used to seperate the sentences

    - Hello how are you. Im learning BERT

    - Hello how are you [SEP] Im learning BERT

- CLS token is used to close the the sentence , also called as Classification token, It is used at starting for classification task

    - [CLS] Hello how are you [SEP] Im learning BERT [SEP]

    - [CLS] token specificall used to classifiation problem

        - sentiment analysis

        - Q and A

        - intent classification etc

    - [CLS] will pass through all BERT layers

    - final layer will pass through pooling layer on top of that will apply tanh

    - only one vector that to [cls] token vector

    - this [cls] token represents whole sentence information

- UNK token indicates that token not present in the vocabualry

- Max length of tokens is 512

- In each sentence the model takes 512 words

- Hello how are you. Im learning BERT

    - the number of words :7

    - but we required 512, after 7 will add [PAD] tokens

    - PAD tokens value is zero

**read the vocabulary**
"""

tokenizer.vocab
# all the words and its index
# the words consider as keys
# total 30522 tokens 0 to 30521
#'[PAD]', '[UNK]', '[CLS]', '[SEP]', '[MASK]'

len(tokenizer.vocab)
# understand the type : dictionary type
# keys are tokens only

tokenizer.vocab.keys()  # only words

# machine
# learning
# machinelearning: machine ##learn ##ing: machine learnig

tokenizer.vocab_size

# tokenizer.vocab ==== enterire words with index
# tokenizer.vocab.keys() ==== only words
# tokenizer.vocab_size

tokenizer.vocab['bed'],tokenizer.vocab['##bed']

tokenizer.tokenize('embeddings')

# we did
tokenizer.vocab['em'],tokenizer.vocab['##bed'],tokenizer.vocab['##ding'],tokenizer.vocab['##s']
#embeddings= em+bed+ding+s
#embeddings is not available
# embeddings =[em,##bed,##ding,##s] =[7861,8270,4667,2015]

tokenizer.vocab['bed']

tokenizer.tokenize('embeddings'),tokenizer.convert_tokens_to_ids(['em','##bed','##ding','##s'])

tokenizer.convert_ids_to_tokens([7861,8270,4667,2015])

tokenizer.convert_tokens_to_string(['em','##bed','##ding','##s'])

tokenizer.convert_tokens_to_ids(['em','##bed','##ding','##s'])
tokenizer.convert_ids_to_tokens([7861,8270,4667,2015])
tokenizer.convert_tokens_to_string(['em','##bed','##ding','##s'])

"""**uncased means No Case sesitive**"""

tokenizer.vocab['machine']

tokenizer.vocab['Machine'] # error

tokenizer.tokenize('machine'),tokenizer.convert_tokens_to_ids('machine')

tokenizer.tokenize('Machine'),tokenizer.convert_tokens_to_ids('Machine')
# In vocabulary the word 'Machine' not there
# UNK token id availabe

tokenizer.vocab['hello'],tokenizer.vocab['##～']

tokenizer.vocab['we'],tokenizer.vocab['are'],tokenizer.vocab['learning'],tokenizer.vocab['bert']

print(tokenizer.vocab['##bed']) # For combining
print(tokenizer.vocab['bed'])  # Normal
print(tokenizer.vocab['##ding'])

"""**tokenizer.tokenize**

- we are tokenize the sentences into words
"""

sentence='we are learning bert embeddings'
tokens=tokenizer.tokenize(sentence)
tokens

sentence1='hello how are you'
tokens=tokenizer.tokenize(sentence1)
tokens

sen2='we are learning bert embeddings.It is interesting'
tokens=tokenizer.tokenize(sen2)
tokens

# GenAI word is not available in vocabulary
# but BERT model divide the GenAi into with
tokenizer.tokenize('GenAI')

tokenizer.tokenize('embeddings')

tokenizer.tokenize('moye moye')

sentence='we are learning bert embeddings'
tokenizer.tokenize(sentence)

tokenizer.tokenize('machine')

tokenizer.tokenize('Machine') # M === > m  model name: uncased

tokenizer.tokenize('machine learning')

"""- whenever we apply tokenize it tries to get the combination of words from vocabuary

- For example **machine learning** is not available in vocabulary

- But mcahine and learning is there

- So it will give two words

- **Machinelearning** is also not there, it tries to break the words

- The breaking words inthe form of # symbol
"""

tokens=tokenizer.tokenize('Machinelearning')
tokens

"""# Sentence tokenize"""

sentence='we are learning bert embeddings'
tokens=tokenizer.tokenize(sentence)
tokens

tokens

# two tokens we need to add to convey that two or more senteces
# cls  and sep

# ['CLS'], '[CLS]'

tokens1=['[CLS]']+tokens+['[SEP]']  #  "[CLS]"
tokens1
# generally CLS AT STARTING ONLY , NOT AT ENDING

# model will takecare all these things automatically

#tokens2=['[cls]']+tokens+['[sep]']
#tokens2 # generally we take CLS and SEP

"""**Add the Pad tokens**"""

tokens2=['[CLS]']+tokens+['[SEP]']+['[PAD]']+['[PAD]']
tokens2
# l1+l2+l3
# every sentence vector should be in equal length
# we need to pad
# here im not adding 512, Im giving an idea how model will add the pad tokens

#tokenizer(sentence)  #model is giving

token_ids=tokenizer.convert_tokens_to_ids(tokens2)
token_ids

"""**Attention mask**

- Token is present read as 1

- PAD token read as 0
"""

#attention_mask where ever tokens present =1 for pad tokens zero
# pads are use to match token length only
attention_mask=[1 if i!='[PAD]' else 0 for i in tokens2]
attention_mask

# this is very fundamental concept in bert

"""- Fisrt we get the tokens

- Next we added extra tokens like CLS,SEP and PAD

- Then we are creating an attention mask : which has 1 and 0

## get the token ids

- we need to retrive the index of each token based on vocab
"""

dir(tokenizer)

tokens2

tokenizer.vocab['we']

ids=tokenizer.convert_tokens_to_ids(tokens2)
ids

tokenizer.convert_ids_to_tokens(ids)

tokenizer.convert_tokens_to_string(tokens2)

tokenizer.convert_tokens_to_ids(tokens2)
tokenizer.convert_tokens_to_string(tokens2)
tokenizer.convert_ids_to_tokens(ids)

dir(tokenizer)
#convert_ids_to_tokens',
#'convert_tokens_to_ids',
#'convert_tokens_to_string',

""" 'convert_ids_to_tokens',

 'convert_tokens_to_ids',

 'convert_tokens_to_string',
"""

token_ids=tokenizer.convert_tokens_to_ids(tokens2)
token_ids



"""when sentence is equal to 512 padding is required or not sir..

sentence words >512

words=512

words<512

Truncate=True pad=True
"""

tokenizer.convert_ids_to_tokens(token_ids)

tokenizer.convert_tokens_to_string(tokens2)

"""- If sentence is available divide into tokens

- Tokens to token ids

- Suppose we have token ids then we can get tokens

- Suppose we have tokesn then we can get senetence
"""

#tokenizer.convert_ids_to_tokens(token_ids)

#[101, 2057, 2024, 4083, 14324, 7861, 8270, 4667, 2015, 102, 0, 0]
tokenizer.vocab['we'],tokenizer.vocab['are'],tokenizer.vocab['learning']

"""we have total 30522 tokens we have, which is our vocabulary

for each token we have one vector having 768 values
"""



"""- Firstly we have sentence

- From sentence will get tokens(words)

- From tokens will get ids

- From ids we get the mask

- In order to get embeddings , we required token_ids and attention mask

- token ids are simply a list

- BERT model is py torch or tensorflow model

- list we need to convert into py torch or tensorflow format
"""

token_ids

# we don't want list type
# we want either array type ==== np
# or tensor type=== pt  or tf

"""**we alreday got following from senetnece by manual way**

- tokens

- token ids

- attention mask

model tokenizer will take setence and provide a dictionary of output
with token ids and attenstion mask

- My loaded BERTModel variable name is : **model**

- My loaded BERTTokenizer variable name is : **tokenizer**

- now pass the senetnece which inside tokenizer model same

- It will give 3 outputs in dictionary

    - token ids that will match what we did till now

    - attention mask that also will match

    - token type ids

        - as we are passing only one senetence , so it wil provide all zeros
"""

tokenizer

sentence

#help(tokenizer)

tokenizer(sentence)
# We added pad but here model did not included padding

#help(tokenizer)

import numpy as np
np.array(token_ids)

"""**unsqueeze**

- Convert 1D tensor to 2D
"""

token_ids

import torch
# token_ids is the variable has our tokens
torch.tensor(token_ids)

"""- we have seen alrady in ML

- recall how we pass a single input for prediction

- recall how we pass a test data for prediction

- it is in a format of list in list

- we passed the 2D data

- [[s1],[s2],[s3]]
"""

import torch
print(torch.tensor(token_ids)) # 1D
torch.tensor(token_ids).unsqueeze(0) # 2D

"""- We need to apply tensor format for both token ids as well as attention mask

- also we need to conver 1D to 2D for both token ids as well as attenstin mask
"""

import torch
torch.tensor(token_ids)  # 1D
torch.tensor(token_ids).unsqueeze(0) # 2D
torch.tensor(token_ids).reshape(1,-1) # 2D

np.expand_dims(np.array([1,2,3,4]),axis=0)

import torch
ids=torch.tensor(token_ids).unsqueeze(0)
mask=torch.tensor(attention_mask).unsqueeze(0)

ids,mask

model

# our model variable name is : model

"""- model will take ids and attenstion mask as inputs

- model will give

    - each token embeddings

    - we have total 8 token other than spl token

    - each token emebdding of 768 vector length

    - so the shape is 8,768

    - we are passing one sentence so the shape is 1,8,768

    - we passed ids which has [CLS],[SEP] and two [PAD]

    - so the final shape is (1,12,768)

- model will give pooler output

    - which indicates sentence level embedding

    - here [CLS] token embeddings pass throuh dense layer + tanh

    - will provide one vectors which is nothing but a pooled vector

    - the whole sentence information stored in [CLS] Embedding

    - shape : (1,768)
"""

ids,attention_mask

# tokenizer model variable
sentence='we are learning bert embeddings'
sentenc1="we are learning bert embeddings.It is interesting"
#
tokenizer(sentence)

"""## Truncation"""

sentenc1="we are learning bert embeddings,It is interesting"
#
print(tokenizer.tokenize(sentenc1))
print(len(tokenizer.tokenize(sentenc1)))
outs=tokenizer(sentenc1,
          padding=True,
          max_length=8,
          truncation=True)
tokenizer.convert_ids_to_tokens(outs['input_ids'])

"""## Padding"""

sen1="we are learning bert embeddings" # 8 ===> 6  truncation
sen2="It is interesting"              # 4 === > 6  padding
sen=[sen1,sen2]
outs=tokenizer(sen,
          padding=True,
          max_length=6,
          truncation=True)
outs

tokenizer.convert_ids_to_tokens(outs['input_ids'][0])

tokenizer.convert_ids_to_tokens(outs['input_ids'][1])

"""**the above thing we did manually**"""

ids,attention_mask

# model variable name is model
out=model(ids,
          attention_mask=mask)
out

len(out)

ids
# 12 tokens
# each token 768
# we passed only one sentence 1
# 1,12,768

out[0]

out[0].shape
# 1   12   768

out[1].shape

len(out[1][0])

model

len(out)

# out[0] means hidden state outputs
out[0].shape
# 1 batch : one sentence
# 12 tokens : 12 tokens include SPL
# 768 vector length of each token

out[1].shape
# Sentece vector

out[0][0].detach().numpy()

tokens2

out[0].ndim

pd.DataFrame([[1,2,3],[10,20,30]])

import pandas as pd
pd.DataFrame(out[0][0].detach().numpy(),
             index=tokens2)

"""**All Together**"""

sentence='we are learning bert embeddings'
tokens=tokenizer.tokenize(sentence)
tokens

tokens1=['[CLS]']+tokens+['[SEP]']
tokens1

tokens2=['[CLS]']+tokens+['[SEP]']+['[PAD]']+['[PAD]']
tokens2

attention_mask=[1 if i!='[PAD]' else 0 for i in tokens2]
attention_mask

token_ids=tokenizer.convert_tokens_to_ids(tokens2)
token_ids


import torch
ids=torch.tensor(token_ids).unsqueeze(0)
mask=torch.tensor(attention_mask).unsqueeze(0)

out=model(ids,attention_mask=mask)
out

"""- Output will give two layers output

    - Hidden layer output

    - Pooler layer output

**Hidden layer output**

- Hidden layer will provide the Word embeddings of each word with 768 shape

- Here we are passing one sentence ,this sentence has 12 tokens each token vector size is 768

- the shape is (1,12,768)

**Pooler layers**

- Here BERT model will apply average pooling which gives average of all 12 token vectors values into a single vectors which has shape 768
"""

#out.items()
out.last_hidden_state
#out[0].shape

# we have 12 tokens are there
# this gives each token embeddvector
# vector has 768 , weights
# 1 batch
# 12 tokens
# 768 values
# 1,12,768



out.last_hidden_state.shape
# out[0]

ids

out.pooler_output.shape
#out[1].shape

out[0] # same as out.last_hidden_state
out[1] # same as out.pooler_output



out[1][0][:10]

"""

**setence_output** : Is simply an array (representation) of the last layer hidden representation of each token which will be of size (batch_size, seq_len, hidden_size)

**pooler_output:** Is representation/embedding of CLS token passed through some more layers Bertpooler, linear/dense and activation function. It is recommended to use this pooler_output as it contains contextualize information of whole sequence."""

import torch
from transformers import BertTokenizer, BertForMaskedLM

# Load pre-trained model and tokenizer
tokenizer = BertTokenizer.from_pretrained('bert-base-uncased')
model = BertForMaskedLM.from_pretrained('bert-base-uncased')

# Example text with a masked token
text = "The cat sat on the [MASK]."

# Tokenize input text
tokenized_text = tokenizer.tokenize(text)
print("Tokenized text:", tokenized_text)

# Convert token to vocabulary indices
masked_index = tokenized_text.index('[MASK]')
indexed_tokens = tokenizer.convert_tokens_to_ids(tokenized_text)

# Convert input to PyTorch tensors
tokens_tensor = torch.tensor([indexed_tokens])

# Predict all tokens
with torch.no_grad():
    outputs = model(tokens_tensor)
    predictions = outputs[0]

# Get the predicted token
predicted_index = torch.argmax(predictions[0, masked_index]).item()
predicted_token = tokenizer.convert_ids_to_tokens([predicted_index])[0]
print("Predicted token:", predicted_token)



"""NLP task
we need to focus how to convert a Text to numbers

this is called as WordEmbeddings

1.Bow
2.tf-idf
3.Word2Vec
4.BERT Transformers

BERT (Bidirectional Encoder Representations from Transformers)

Package name: Transformers
Developed by: Huggingface ==== Google
=========================================
Understanding the BERT models

1.install transformers
2.we need to import
   A) BertModel is used to access the Models(1M)
   B) BertTokenizer is used to access the vocab and    tokens
3. BertModel here we access
   model name: 'bert-base-uncased'

4. Input layer === Encoder layer === Pooler layer

5. the total tokens or words= 30522
6. the maximum sentence token size= 512
7. each token vector size = 768
8. Tokenizer has a vocab list
   A)which has token and its index
   B) 30522 tokens are available
   C) [PAD]
   D) [CLS]
   E) [SEP]
   D) [UNK]
   E) [MSK]
9) Best part here is
   when we want to divide a sentence into tokens
   A) first it will search is that word available in vocab list
   B) If it is not available
      it is try to tokenize the word with the tokens
      which is available in vocabulary list

   ex: 'moye' ==== >   'mo'+'#ye'

10) Any senetence can tokenize

11) Sentence= 'we are learning BERT Embeddings'

12) ['we','are','learning','BERT','EM','#bed','#dings']

13) Here we are adding some spl tokens [cls],[sep],[PAD]

14) ['[cls]','we','are','learning','BERT',
    'EM','#bed','#dings','[SEP]','[PAD]']

15) Attention mask:
     where ever [PAD] is make it as 0, other wise 1
    
16) Attention mask:[1,1,1,1,1,1,1,1,1,0]

17) Token ids: [101, 2057, 2024, 4083, 14324, 7861, 8270, 4667, 2015, 102, 0, 0]

18) In order to get the Vector we need to pass
    A) Attention mask of sentence
    B) TokenIds of sentence

19) The sentence has 7 words+3 spl tokens , we will get seven vectors
    each vector size is 768

    shape (10,768)

20) we will apply the pooler layer on 10 vectors finally we will get
    one sentence vectors

21) token ids should be in the form of
    either pytorch tensors
    or tensorflow tensors

   A) LIST ======== > [1,2,3,4]
   B) numpy array === >  array([1,2,3,4])
   C) Pytorch ===== > tensor([1,2,3,4])
   D) Tensorflow==== > tensor([1,2,3,4],dtype=int64)








"""

sen1="we are learning bert embeddings" # 8 ===> 6  truncation            # 4 === > 6  padding
outs=tokenizer(sen1,
          padding=True,
          max_length=6,
          truncation=True)
outs

def f1(**kwargs):
    print(kwargs)

f1(a=10,b=20)

def f1(*args):
    print(args)

f1(10,20)

f1(**{'a':10,'b':20})

#outs['input_ids']
#model(outs['input_ids'],outs['attention_mask'])
sen1="we are learning bert embeddings" # 8 ===> 6  truncation            # 4 === > 6  padding
outs=tokenizer(sen1,
          padding=True,
          max_length=6,
          truncation=True,
          return_tensors='pt')

model(**outs)

